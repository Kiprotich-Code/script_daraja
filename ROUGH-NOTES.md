The longterm plan is to make it easier to configure M-pesa payments integration
Very simple steps, every vibe 'guru' won't miss:
1. Update your .env
2. Install python, if you don't already have it
3. Read the freaking docs - 
4. Install requirements (maybe or maybe not)
5. Run test to specific choke points i.e
- run all tests (end-to-end)
- run auth --only
- run auth stkpush 
- run -a to x 
- run -k to z 
